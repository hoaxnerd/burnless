{
  "name": "burnless",
  "version": "0.1.3",
  "description": "The Burnless CLI — drive your Burnless instance from the terminal over MCP",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "burnless": "./dist/index.thin.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "comment": "No runtime dependencies: tsup bundles everything into dist/ (the thin bin imports only Node built-ins). The packages below are build-time only, hence devDependencies — keeping them out of the published runtime deps (workspace:* refs are unresolvable on npm).",
  "devDependencies": {
    "@burnless/ai": "workspace:*",
    "@burnless/db": "workspace:*",
    "@burnless/types": "workspace:*",
    "@clack/prompts": "^1.5.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@types/node": "^22.19.15",
    "commander": "^14.0.0",
    "drizzle-orm": "^0.38.0",
    "ora": "^9.4.0",
    "picocolors": "^1.1.1",
    "tsup": "^8.5.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "zod": "^3.25.76"
  }
}
