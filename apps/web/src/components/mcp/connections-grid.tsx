"use client";

/**
 * Connections page body — header + connection-card grid + Add-connection modal.
 *
 * Stub for now (Plan 2 Task 1 keeps the tree green); Task 2 builds the real
 * grid pixel-exact to `placement.html`.
 */
export function ConnectionsGrid() {
  return null;
}
