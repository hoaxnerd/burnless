"use client";

/**
 * AddConnectionModal — config-paste-first add flow (mockup: add-connection.html).
 *
 * Null-rendering stub so the Connections grid compiles (Plan 2 Task 2);
 * Task 3 replaces this with the real modal.
 */
export interface AddConnectionModalProps {
  open: boolean;
  onClose: () => void;
  onCreated: () => void;
}

export function AddConnectionModal(_props: AddConnectionModalProps) {
  return null;
}
